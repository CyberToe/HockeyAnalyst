{
  "version": 2,
  "buildCommand": "npm run build:all && npx prisma generate && cp -r backend/dist api/backend-dist",
  "installCommand": "npm install",
  "outputDirectory": "frontend/dist",
  "framework": null,
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "/api/index.js"
    },
    {
      "handle": "filesystem"
    },
    {
      "src": "/(.*)",
      "dest": "/index.html"
    }
  ]
}
