{
  "version": 2,
  "buildCommand": "npm run build:all && npx prisma generate && copy backend/dist/routes/*.js api/routes/ && copy backend/dist/middleware/*.js api/middleware/ && copy backend/dist/lib/*.js api/lib/",
  "installCommand": "npm install",
  "outputDirectory": "frontend/dist",
  "framework": null,
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "/api/index.js"
    },
    {
      "handle": "filesystem"
    },
    {
      "src": "/(.*)",
      "dest": "/index.html"
    }
  ]
}
