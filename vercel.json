{
  "version": 2,
  "buildCommand": "npm run build:all && npx prisma generate && node scripts/copy-backend.js",
  "installCommand": "npm install",
  "outputDirectory": "frontend/dist",
  "framework": null,
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "/api/index.js"
    },
    {
      "handle": "filesystem"
    },
    {
      "src": "/(.*)",
      "dest": "/index.html"
    }
  ]
}
