{
  "version": 2,
  "buildCommand": "npm run build:all && npx prisma generate && mkdir -p api/routes api/middleware api/lib && cp backend/dist/routes/*.js api/routes/ || true && cp backend/dist/middleware/*.js api/middleware/ || true && cp backend/dist/lib/*.js api/lib/ || true",
  "installCommand": "npm install",
  "outputDirectory": "frontend/dist",
  "framework": null,
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "/api/index.js"
    },
    {
      "handle": "filesystem"
    },
    {
      "src": "/(.*)",
      "dest": "/index.html"
    }
  ]
}
